// Package filter is the metadata predicate language the engine applies to
// candidates: comparison, set, string, geo and boolean operators, built from
// JSON or from Go and evaluated against a document's metadata.
//
// Tower layer: L1 engine.
package filter
